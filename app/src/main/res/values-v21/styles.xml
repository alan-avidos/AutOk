<resources>

        <style name="EditTextHint" parent="Widget.Design.TextInputLayout">
                <!-- Customize your theme here. -->
                <item name="colorAccent">@android:color/white</item>
                <item name="android:textColorHint">@color/colorAppOrange</item>
                <item name="android:colorControlNormal">@color/colorAppWhiteText</item>
                <item name="android:colorControlActivated">@color/colorAppOrange</item>
                <item name="android:colorControlHighlight">@color/colorAppWhiteText</item>
        </style>

        <style name="EditTextHintBlack" parent="Widget.Design.TextInputLayout">
                <!-- Customize your theme here. -->
                <item name="colorAccent">@color/colorAppGrayText</item>
                <item name="android:textColorHint">@color/colorAppGrayText</item>
                <item name="android:colorControlNormal">@color/colorAppGrayText</item>
                <item name="android:colorControlActivated">@color/colorAppGrayText</item>
                <item name="android:colorControlHighlight">@color/colorAppGrayText</item>
        </style>
</resources>
